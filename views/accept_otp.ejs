<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <%-include('cdn.ejs') %> 

    <style>
        body {
            background-color: #f8f9fa;
        }
        .otp-container {
            max-width: 400px;
            margin: 50px auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="otp-container text-center">
            <h2 class="mb-3 text-primary">OTP Verification</h2>
            <hr>

            <form action="/otpverified" method="post">
                <div class="mb-3">
                    <label class="form-label">Enter OTP</label>
                    <input type="text" name="otp" class="form-control text-center" required>
                </div>

                <button type="submit" class="btn btn-success w-100">Verify OTP</button>
            </form>

            <div class="mt-3">
                <a href="/resend-otp" class="text-danger">Resend OTP</a>
            </div>
        </div>
    </div>

</body>
</html>
